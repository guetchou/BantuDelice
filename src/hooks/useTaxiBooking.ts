
export const useTaxiBooking = () => {
  return {
    drivers: {
      // Placeholders pour éviter les dépendances circulaires
    },
    rides: {
      // Placeholders pour éviter les dépendances circulaires
    }
  };
};
