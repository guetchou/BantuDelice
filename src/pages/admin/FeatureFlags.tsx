
import React from "react";
import FeatureFlags from "@/pages/FeatureFlags";

// This is a wrapper component to ensure proper imports in the admin section
export default function AdminFeatureFlags() {
  return <FeatureFlags />;
}
