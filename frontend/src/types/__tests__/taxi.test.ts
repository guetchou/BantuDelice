import { RideStatus, VehicleType, PaymentMethod, RideData, LocationInfo, DriverInfo, VehicleInfo, PassengerInfo, RidePricing, PaymentInfo, RouteInfo, TaxiRatingInfo, CreateRideRequest, UpdateRideRequest, RideFilters, RideSearchParams, RideResponse, RideListResponse, DriverResponse, DriverListResponse, RideStatsResponse, RideStats, RideNotification, RideNotificationResponse } from '../taxi';

describe('taxi types', () => {
  it('should have valid RideStatus type', () => {
    // Test de base pour vérifier que le type existe
    const testRideStatus: RideStatus = {} as RideStatus;
    expect(testRideStatus).toBeDefined();
  });

  it('should have valid VehicleType type', () => {
    // Test de base pour vérifier que le type existe
    const testVehicleType: VehicleType = {} as VehicleType;
    expect(testVehicleType).toBeDefined();
  });

  it('should have valid PaymentMethod type', () => {
    // Test de base pour vérifier que le type existe
    const testPaymentMethod: PaymentMethod = {} as PaymentMethod;
    expect(testPaymentMethod).toBeDefined();
  });

  it('should have valid RideData type', () => {
    // Test de base pour vérifier que le type existe
    const testRideData: RideData = {} as RideData;
    expect(testRideData).toBeDefined();
  });

  it('should have valid LocationInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testLocationInfo: LocationInfo = {} as LocationInfo;
    expect(testLocationInfo).toBeDefined();
  });

  it('should have valid DriverInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testDriverInfo: DriverInfo = {} as DriverInfo;
    expect(testDriverInfo).toBeDefined();
  });

  it('should have valid VehicleInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testVehicleInfo: VehicleInfo = {} as VehicleInfo;
    expect(testVehicleInfo).toBeDefined();
  });

  it('should have valid PassengerInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testPassengerInfo: PassengerInfo = {} as PassengerInfo;
    expect(testPassengerInfo).toBeDefined();
  });

  it('should have valid RidePricing type', () => {
    // Test de base pour vérifier que le type existe
    const testRidePricing: RidePricing = {} as RidePricing;
    expect(testRidePricing).toBeDefined();
  });

  it('should have valid PaymentInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testPaymentInfo: PaymentInfo = {} as PaymentInfo;
    expect(testPaymentInfo).toBeDefined();
  });

  it('should have valid RouteInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testRouteInfo: RouteInfo = {} as RouteInfo;
    expect(testRouteInfo).toBeDefined();
  });

  it('should have valid TaxiRatingInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testTaxiRatingInfo: TaxiRatingInfo = {} as TaxiRatingInfo;
    expect(testTaxiRatingInfo).toBeDefined();
  });

  it('should have valid CreateRideRequest type', () => {
    // Test de base pour vérifier que le type existe
    const testCreateRideRequest: CreateRideRequest = {} as CreateRideRequest;
    expect(testCreateRideRequest).toBeDefined();
  });

  it('should have valid UpdateRideRequest type', () => {
    // Test de base pour vérifier que le type existe
    const testUpdateRideRequest: UpdateRideRequest = {} as UpdateRideRequest;
    expect(testUpdateRideRequest).toBeDefined();
  });

  it('should have valid RideFilters type', () => {
    // Test de base pour vérifier que le type existe
    const testRideFilters: RideFilters = {} as RideFilters;
    expect(testRideFilters).toBeDefined();
  });

  it('should have valid RideSearchParams type', () => {
    // Test de base pour vérifier que le type existe
    const testRideSearchParams: RideSearchParams = {} as RideSearchParams;
    expect(testRideSearchParams).toBeDefined();
  });

  it('should have valid RideResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testRideResponse: RideResponse = {} as RideResponse;
    expect(testRideResponse).toBeDefined();
  });

  it('should have valid RideListResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testRideListResponse: RideListResponse = {} as RideListResponse;
    expect(testRideListResponse).toBeDefined();
  });

  it('should have valid DriverResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testDriverResponse: DriverResponse = {} as DriverResponse;
    expect(testDriverResponse).toBeDefined();
  });

  it('should have valid DriverListResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testDriverListResponse: DriverListResponse = {} as DriverListResponse;
    expect(testDriverListResponse).toBeDefined();
  });

  it('should have valid RideStatsResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testRideStatsResponse: RideStatsResponse = {} as RideStatsResponse;
    expect(testRideStatsResponse).toBeDefined();
  });

  it('should have valid RideStats type', () => {
    // Test de base pour vérifier que le type existe
    const testRideStats: RideStats = {} as RideStats;
    expect(testRideStats).toBeDefined();
  });

  it('should have valid RideNotification type', () => {
    // Test de base pour vérifier que le type existe
    const testRideNotification: RideNotification = {} as RideNotification;
    expect(testRideNotification).toBeDefined();
  });

  it('should have valid RideNotificationResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testRideNotificationResponse: RideNotificationResponse = {} as RideNotificationResponse;
    expect(testRideNotificationResponse).toBeDefined();
  });
});
