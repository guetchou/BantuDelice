import { ColisStatus, PackageType, DeliverySpeed, ServiceType, ColisData, SenderInfo, RecipientInfo, PackageInfo, ServiceInfo, PricingInfo, TrackingInfo, TrackingEvent, ColisPaymentMethod, CreateColisRequest, UpdateColisRequest, ColisFilters, ColisSearchParams, ColisResponse, ColisListResponse, TrackingResponse, ColisStatsResponse, ColisStats, ColisNotification, ColisNotificationResponse } from '../colis';

describe('colis types', () => {
  it('should have valid ColisStatus type', () => {
    // Test de base pour vérifier que le type existe
    const testColisStatus: ColisStatus = {} as ColisStatus;
    expect(testColisStatus).toBeDefined();
  });

  it('should have valid PackageType type', () => {
    // Test de base pour vérifier que le type existe
    const testPackageType: PackageType = {} as PackageType;
    expect(testPackageType).toBeDefined();
  });

  it('should have valid DeliverySpeed type', () => {
    // Test de base pour vérifier que le type existe
    const testDeliverySpeed: DeliverySpeed = {} as DeliverySpeed;
    expect(testDeliverySpeed).toBeDefined();
  });

  it('should have valid ServiceType type', () => {
    // Test de base pour vérifier que le type existe
    const testServiceType: ServiceType = {} as ServiceType;
    expect(testServiceType).toBeDefined();
  });

  it('should have valid ColisData type', () => {
    // Test de base pour vérifier que le type existe
    const testColisData: ColisData = {} as ColisData;
    expect(testColisData).toBeDefined();
  });

  it('should have valid SenderInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testSenderInfo: SenderInfo = {} as SenderInfo;
    expect(testSenderInfo).toBeDefined();
  });

  it('should have valid RecipientInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testRecipientInfo: RecipientInfo = {} as RecipientInfo;
    expect(testRecipientInfo).toBeDefined();
  });

  it('should have valid PackageInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testPackageInfo: PackageInfo = {} as PackageInfo;
    expect(testPackageInfo).toBeDefined();
  });

  it('should have valid ServiceInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testServiceInfo: ServiceInfo = {} as ServiceInfo;
    expect(testServiceInfo).toBeDefined();
  });

  it('should have valid PricingInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testPricingInfo: PricingInfo = {} as PricingInfo;
    expect(testPricingInfo).toBeDefined();
  });

  it('should have valid TrackingInfo type', () => {
    // Test de base pour vérifier que le type existe
    const testTrackingInfo: TrackingInfo = {} as TrackingInfo;
    expect(testTrackingInfo).toBeDefined();
  });

  it('should have valid TrackingEvent type', () => {
    // Test de base pour vérifier que le type existe
    const testTrackingEvent: TrackingEvent = {} as TrackingEvent;
    expect(testTrackingEvent).toBeDefined();
  });

  it('should have valid ColisPaymentMethod type', () => {
    // Test de base pour vérifier que le type existe
    const testColisPaymentMethod: ColisPaymentMethod = {} as ColisPaymentMethod;
    expect(testColisPaymentMethod).toBeDefined();
  });

  it('should have valid CreateColisRequest type', () => {
    // Test de base pour vérifier que le type existe
    const testCreateColisRequest: CreateColisRequest = {} as CreateColisRequest;
    expect(testCreateColisRequest).toBeDefined();
  });

  it('should have valid UpdateColisRequest type', () => {
    // Test de base pour vérifier que le type existe
    const testUpdateColisRequest: UpdateColisRequest = {} as UpdateColisRequest;
    expect(testUpdateColisRequest).toBeDefined();
  });

  it('should have valid ColisFilters type', () => {
    // Test de base pour vérifier que le type existe
    const testColisFilters: ColisFilters = {} as ColisFilters;
    expect(testColisFilters).toBeDefined();
  });

  it('should have valid ColisSearchParams type', () => {
    // Test de base pour vérifier que le type existe
    const testColisSearchParams: ColisSearchParams = {} as ColisSearchParams;
    expect(testColisSearchParams).toBeDefined();
  });

  it('should have valid ColisResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testColisResponse: ColisResponse = {} as ColisResponse;
    expect(testColisResponse).toBeDefined();
  });

  it('should have valid ColisListResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testColisListResponse: ColisListResponse = {} as ColisListResponse;
    expect(testColisListResponse).toBeDefined();
  });

  it('should have valid TrackingResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testTrackingResponse: TrackingResponse = {} as TrackingResponse;
    expect(testTrackingResponse).toBeDefined();
  });

  it('should have valid ColisStatsResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testColisStatsResponse: ColisStatsResponse = {} as ColisStatsResponse;
    expect(testColisStatsResponse).toBeDefined();
  });

  it('should have valid ColisStats type', () => {
    // Test de base pour vérifier que le type existe
    const testColisStats: ColisStats = {} as ColisStats;
    expect(testColisStats).toBeDefined();
  });

  it('should have valid ColisNotification type', () => {
    // Test de base pour vérifier que le type existe
    const testColisNotification: ColisNotification = {} as ColisNotification;
    expect(testColisNotification).toBeDefined();
  });

  it('should have valid ColisNotificationResponse type', () => {
    // Test de base pour vérifier que le type existe
    const testColisNotificationResponse: ColisNotificationResponse = {} as ColisNotificationResponse;
    expect(testColisNotificationResponse).toBeDefined();
  });
});
