
{
  "name": "buntudelice-backend",
  "version": "1.0.0",
  "description": "Backend API for Buntudelice application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prod": "pm2 start ecosystem.config.js --env production",
    "lint": "eslint . --ext .js",
    "test": "jest",
    "health-check": "node -e \"require('http').get('http://localhost:5000/health', (res) => console.log(res.statusCode === 200 ? 'OK' : 'FAIL'))\""
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.6.3",
    "pm2": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Ambangue/BuntuDelice42.git"
  },
  "author": "Buntudelice Team",
  "license": "MIT"
}
